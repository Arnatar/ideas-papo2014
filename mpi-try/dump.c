
  // init all to zero
  // int myArray[10] = { 0 };
  // pr(myArray,10);

  // int values[] = { 1, 2, 3 };
  size_t y_len = 3;
  size_t x_len = 2;
  int values2[y_len][x_len];

  int r[5]; 
  for(int i=0; i<5; i++) {
    r[i] = rand_int(100,1);
  }
  // pr(r);

  // int arr[] = {1,2,3};
  // pr(arr);
  // int c[5] = {1,2,3,4,5};
  int balance[] = {1000, 2, 3, 17, 50};
  // pr(c);
  // pr(c);
  // int my_sum = sum(c);
  // pri("sum",sum));
  // int s = sum(balance);
  // pri(sum(balance));
  int number = sum(balance);
  pri(number);
  pre();

  // pri(sizeof *(balance) );
  my_print(balance);
  // pri(my_sizeof(balance));



  // int values2[y_len][2] = { {1,4}, {2,2},{3,3} };
  // pr(values);
  // pr2dim(values2,3);


